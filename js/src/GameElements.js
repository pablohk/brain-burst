// x and y are the index of grid, not pixels of canvas
function GameElements (x,y,width,height,img,id){
  this.x=x;
  this.y=y;
  this.width=width;
  this.height=height;
  this.img= img;
  this.id=id;
}
